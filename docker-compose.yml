version: '3.8'
services:
  taskrelay:
    build: .
    container_name: taskrelay
    ports:
      - "4010:4010"
    volumes:
      - taskrelay_data:/app/data
    environment:
      - DB_PATH=/app/data/taskrelay.db
      - PORT=4010
    restart: unless-stopped

volumes:
  taskrelay_data:
